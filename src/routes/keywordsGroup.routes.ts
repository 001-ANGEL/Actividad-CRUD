import { Router } from "express";


const router = Router();

router.post("/api/keyword-groups", );

router.get("/api/keyword-groups", );

router.get("/api/keyword-groups/:id", );

router.put("/api/keyword-groups/:id", );

router.delete("/api/keyword-groups/:id", );

export default router;
