import { Router } from "express";


const router = Router();

router.post("/api/written-content", );

router.get("/api/written-content", );

router.get("/api/written-content/:id", );

router.put("/api/written-content/:id", );

router.delete("/api/written-content/:id", );

export default router;
